def cop(a,b):
    return gcd(a,b)==1
def gcd(a,b):
    while(b!=0):
        rem=a%b
        temp=b
        b=rem
        a=temp
    return a
n=int(input("Enter the number: "))
for a in range(2,21):
    for b in range(2,21):
        for c in range(2,21):
            if (c*c+b*b==a*a and cop(a,b) and cop(b,c) and (a,c)):
                print(a,b,c)
